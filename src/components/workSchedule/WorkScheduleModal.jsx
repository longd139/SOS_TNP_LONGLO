import React from 'react'

export default function WorkScheduleModal({
    isOpen,
    onClose,
    onSubmit,
    initialData = null,
    mode = 'create'
}) {
    
    return (
        <div>
            MODAL
        </div>
    )
}
